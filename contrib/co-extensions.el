;; Extensions are in emacs_paths/contrib/extensions

;; Pre extensions are loaded *before* the co/packages
(defvar co/pre-extensions '())

;; Pre extensions are loaded *after* the co/packages
(defvar co/post-extensions '())

(provide 'co-extensions)
