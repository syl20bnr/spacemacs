(require 'edts-start)

;; (setq edts-log-level 'debug)

(setq edts-projects
      '(( ;; Erlang tests.
         (name       . "erlang")
         (root       . "/home/sbenner/dev/projects/erlang")
         (node-sname . "erltests"))
        ))
