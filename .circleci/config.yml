---
# Circle CI YAML file
version: 2.0
jobs:
  build:
    docker:
      - image: jare/spacemacs-circleci
    working_directory: /root/.emacs.d
    steps:
      - checkout
      - run: ./.circleci/run
