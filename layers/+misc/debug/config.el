(defvar debug-mode 'gdb
  "Options are `gdb' to run specific debugger.")

(defvar debug-prog nil
  "Variable that holds the path to the debugger executable.")

(defvar debug-target nil
  "Executable that should be debugged.")
